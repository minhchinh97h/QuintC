import React, {memo} from "react"
import { Text } from "react-native"

const JournalScreen = () => {
    return(
        <Text>
            Journal Screen
        </Text>
    )
}

export default memo(JournalScreen)