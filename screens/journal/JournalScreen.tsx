import React, {memo} from "react"
import { Text } from "react-native"

const JournalScreen = () => {
    return(
        <Text>
            Testing
        </Text>
    )
}

export default memo(JournalScreen)