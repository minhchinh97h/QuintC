import React, { memo } from "react"
import { Text } from "react-native"

const WeekTabScreen = () => {
    return (
        <Text>
            Week Tab Screen
        </Text>
    )
}

export default memo(WeekTabScreen)